cache: pip
addons:
  apt:
    packages:
      - bedtools
language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
script:
  - set -e
  - python setup.py install
  - pwd
  - nosetests .
