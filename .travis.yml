sudo: required
cache: pip
addons:
  apt:
    packages:
      - bedtools
language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
script:
  - set -e
  - python setup.py install
  - python setup.py build_ext --inplace
  - nosetests
